# Tiny Model Configuration
# ~10M parameters, for testing and CI/CD

name: tiny
vocab_size: 32000

# Model dimensions
d_model: 256
n_layers: 4
n_heads: 4
d_ff: 1024

# Sequence settings
max_seq_len: 512

# Regularization
dropout: 0.0

# Architecture options
use_rmsnorm: true
use_rope: true

# Estimated memory: ~1GB
